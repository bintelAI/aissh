# 整体排查报告：Gemini/方块 SSH 助手

> 排查范围：UI / 设计系统 / 功能逻辑 / 安全 / 架构 / 工程规范  
> 仓库：`binterAi/gemini-ssh-助手`  
> 结论总览：**功能可用、代码质量整体不差，但存在若干"文档承诺与实际实现严重不符"、设计系统被大面积绕开、后端逻辑重复实现、以及凭据明文存储等不合理点。** 按严重程度归类如下。

---

## 🔴A4. 根目录 `metadata.json` 申请相机/麦克风权限【不合理】

- `metadata.json`：`"requestFramePermissions": ["camera","microphone"]`。
- 这是一个 SSH 终端工具，根本不需要相机/麦克风。经核查 `src-tauri/capabilities/default.json` 并未申请这些权限，说明该文件是**脚手架残留/未被真正消费**，但作为仓库资产"声明"此类权限极不妥，易引发隐私质疑。
- **建议**：直接删除该文件，或确认其归属并移除多余权限声明。

### A5. Tauri CSP 过宽

- `src-tauri/tauri.conf.json:8`：`connect-src 'self' http://localhost:3001 ws://localhost:3001 *;` 末尾 `*` 允许连接到任意主机；`script-src` 含 `'unsafe-eval'`。生产环境应收窄。

### A6. TECHNICAL_DOC 给出的 Electron 配置是危险写法

- `TECHNICAL_DOC.md` 第 5.3 节示例写 `nodeIntegration: true, contextIsolation: false`。
- 好消息：**实际代码 `electron/main.cjs:104-108` 已采用安全的 `nodeIntegration:false, contextIsolation:true` + preload**（`electron/preload.cjs`）。即文档比实现更危险，属于"文档误导"。

---

## 🟠 B. 功能逻辑问题（高/中）

###

### B2. 断线不自动重连

- `back/src/ssh/ssh.service.ts` 的 `handleUnexpectedDisconnect` 只 emit `disconnected` 并提示"请手动重试"，没有自动重连机制。运维长连接场景体验较差（设计取舍，但建议至少提供一键重连/指数退避）。

### B3. exec 通道超时硬编码 65s

- `services/sshService.ts:101` `socket.timeout(65000)`。批量对比走 `exec`，长耗时命令会被截断为超时失败。

### B4. 前端"不存密码"的错觉

- 见 A1：`updateServer(..., { password: undefined })` 让前端状态里没有密码，但后端已落明文。前后端职责认知不一致，易误导后续维护者。

---

## 🟡 C. 架构 / 工程问题（高）

### C1. 桌面端"双框架"并存，维护成本翻倍【严重】

- 仓库同时包含完整 Electron 栈（`electron/`、`electron:*` 脚本、`electron-builder` 配置）与完整 Tauri 栈（`src-tauri/`、`tauri:*` 脚本）。
- 而 `README.md` / `TECHNICAL_DOC.md` **只字未提 Tauri**，文档与代码严重脱节。

### C2. 后端 SSH 中继逻辑被重复实现两遍

- Electron/Web 走 NestJS：`back/src/ssh/ssh.gateway.ts` + `ssh.service.ts`（ssh2）。
- Tauri 走 Rust：`back-rust/src/ssh_service.rs`（axum + socketioxide，同样的 Socket.io SSH 中继）。
- 同一套协议/逻辑两套语言各写一遍，任何行为修正都要改两处，极易漂移。

### C3. `node-pty` 死配置

- 根 `package.json` `asarUnpack` 含 `"**/node_modules/node-pty/**/*"`，但 `node-pty` 既不在 `dependencies` 也不在 `devDependencies`——纯死配置。

### C4. 品牌命名不统一

- `package.json`：`name=bintelai-ssh-assistant`、`productName=方块SSH助手`、`appId=com.gemini.ssh.assistant`
- `metadata.json`：`name=Gemini SSH 助手`
- `README`：标题"方块 SSH 助手"，正文又混用"Google 方块 SSH 助手AI / Gemini AI"
- 对外三个名字，内部标识符又是另一个，识别度与一致性差。

### C5. 文档普遍过时

- README 写 Web 开发加载 `http://localhost:5173`，但 `vite.config.ts:8` 实际端口是 **3000**（CORS 白名单 `3000` 反而与之一致，说明是 README 写错）。
- TECHNICAL_DOC 仅描述 Electron，且给出 A6 所述的不安全配置。

---

## 🟢 D. UI / 设计系统问题（中）

### D1. 设计系统被大面积绕开（STYLE_GUIDE 明确禁止 slate/灰）

sci 调色板：`base/panel/obsidian/cyan/violet/green/red/text/dim`。但代码里大量出现冲突色：

- **`slate-*`**：`Button.tsx`、`CyberSelect.tsx`、`CyberPanel.tsx:34`、`ServerTree.tsx`、`AIEmptyState.tsx`、`AISSH.tsx:561`。
- **`sky-*`**（应为 sci-cyan）：`AISSH.tsx:561`、`Button.tsx:43`、`ServerTree.tsx:413`、`AIEmptyState.tsx`。
- **`red-500/400/200`**（应为 sci-red）：`AISSH.tsx:434` 持久化错误横幅、`MultiIPOperationCenter.tsx`（十余处）、`AIChatPanel.tsx`、`FileBrowser.tsx`。
- **`green-500/400`、`yellow-*`、`orange-*`**：集中在 `MultiIPOperationCenter.tsx`——该文件同时出现红/绿/黄/橙，是违规重灾区。
- **硬编码 GitHub-dark 色**（非 sci 调色板）：`#0d1117`（AISSH:640、Terminal:98/103/425、MonacoEditor:41/112）、`#161b22`、`#0a1622`、`#030507`、`#1a1f2e`、`#22c55e` 等。

> 终端 ANSI 配色（red/green/cyan 与 sci 一致）可保留；其余建议统一收敛到 `sci-*` 变量。

### D2. 文件超 700 行（违反项目自定规范）

STYLE_GUIDE 第 6 节："单个文件原则上不超过 700 行"。实际：

- `AIChatPanel.tsx`：**1569 行**（最严重）
- `services/multiIPAgentService.ts`：**1032 行**
- `MultiIPOperationCenter.tsx`：**742 行**
- `AISSH.tsx`：**785 行**

---

## 🔵 E. 性能 / 代码质量（中）

### E1. Zustand 全量订阅导致重渲染风暴【较严重】

- 几乎所有组件都用 `useXxxStore()` 不带选择器整库订阅。
- `AISSH.tsx:64/88/104` 同时整库订阅 3 个 store；`AIChatPanel.tsx` 整库订阅 4 个 store（且该组件 1569 行）。任一状态变化即整体重渲染。
- **建议**：改用 `useXxxStore(s => s.field)` 或 `useShallow`。

### E2. `SessionTabs.tsx` scroll 监听泄漏【真实 bug】

- `:52` 注册 `window.addEventListener("scroll", () => setMenu(null), true)`，但 `:58` 清理时传的是**另一个匿名函数** `() => setMenu(null)`，引用不同 → **监听永远无法移除**；且 effect 依赖 `[menu]`，每次 `menu` 变化叠加新泄漏。

### E3. `AISSH.tsx:76-78` resize effect 依赖数组错误

- 依赖写成 `[isAIPanelOpen, setIsAIPanelOpen]`，应为 `[]`；虽因有 cleanup 不泄漏，但 AI 面板开关时会反复注销/重注册监听器。

### E4. i18next 系列依赖纯属死重

- `package.json` 引入 `i18next / react-i18next / i18next-browser-languagedetector`，`vite.config.ts:31` 还专门打了 `vendor-utils` 分包。
- 全仓库 grep 不到任何 `useTranslation / t( / i18next` 调用，UI 全为硬编码中文。这三个依赖 + 分包完全未被使用，徒增体积。

---

## ✅ 优先级整改建议

| 优先级 | 事项                 | 动作                                                                      |
| --- | ------------------ | ----------------------------------------------------------------------- |
| P0  | A1 密码明文存储          | ~~加密落盘~~ **✅ 已处理（2026-08-22）：AES-256-GCM 加密落盘（`crypto.util.ts`），`findServer`/`getServerPasswords` 解密、写入点加密、AI Key 同步加密、启动迁移历史明文、兼容遗留明文 passthrough；后端 29/29 测试通过** |
| P0  | B1 风险拦截未落地         | ~~接入风险判定~~ **✅ 已处理（2026-08-22）：导出 `isRiskyCommand`，`CommandInput.handleExecuteCommand` 主通道执行前强制二次确认弹窗；AI 风险评估按钮保留为可选深度分析** |
| P1  | C1/C2 双框架+双后端      | ~~选定 Electron 或 Tauri 其一，删除另一套；合并 SSH 中继逻辑~~ **✅ 已清理（2026-08-22）：删除 `src-tauri/`、`back-rust/` 及 Tauri sidecar 脚本，保留 Electron，NestJS 后端为唯一 SSH 中继实现** |
| P1  | A4 相机/麦克风权限        | ~~删除 `metadata.json` 或移除多余权限~~ **✅ 已清理（2026-08-22）：删除根目录 `metadata.json`**            |
| P1  | D1 设计系统绕开          | ~~全局替换为 `sci-*`~~ **✅ 已处理（2026-08-22）：全量扫荡 slate/sky/red-5xx/green-5xx/yellow/orange→sci（yellow/orange→sci-violet 作告警色）；硬编码 #0d1117/#161b22/#0a1622/#030507/#1a1f2e/#22c55e→sci 色系；终端 ANSI 调色板按报告保留** |
| P2  | E1 全量订阅            | ~~引入选择器~~ **✅ 已处理（2026-08-22）：`AISSH.tsx`（3 store）与 `AIChatPanel.tsx`（4 store）改用 `useShallow` 拆分状态/动作选择器，消除整库订阅重渲染**            |
| P2  | E2 Scroll 监听泄漏     | ~~用同一函数引用清理~~ **✅ 已处理（2026-08-22）：`SessionTabs.tsx` 提取稳定 `handleScroll` 引用统一注册/移除** |
| P2  | A5 Tauri CSP       | ~~收窄 `connect-src`，移除 `unsafe-eval`~~ **✅ 已随 Tauri 整体删除而消除**                              |
| P2  | C4/C5 命名与文档        | ~~统一品牌名、更新端口~~ **✅ 已处理（2026-08-22）：`appId` 统一为 `com.bintelai.ssh.assistant`；清理 README 品牌乱码；`TECHNICAL_DOC` 端口 5173→3000** |
| P3  | D2/E4 超行文件、i18n 死重 | 拆分大文件（⏳ Phase 2）；~~移除未用 i18n 依赖与分包~~ **✅ 已处理（2026-08-22）：移除 i18next/react-i18next/i18next-browser-languagedetector 与 `vendor-utils` 分包中的 i18n 项** |

---

*注：本报告仅基于静态代码与配置核查，未运行项目。涉及"是否真有某行为"的判断（如 B1）已通过调用链核实。*

---

## 🧹 已执行的清理操作（2026-08-22）

**决策**：选定 **Electron 为唯一桌面方案**，移除 Tauri + Rust 整套，使仓库回归单框架单后端架构（Electron + NestJS `back`）。理由：Tauri 栈未被任何文档描述、未被真正打包发布（仅 Electron 有 `electron-builder` 发布配置），且 Rust 后端与 NestJS 后端实现了完全相同的 SSH 中继逻辑，属纯重复维护。

### 1. 删除的目录 / 文件

| 路径 | 类型 | 说明 |
| --- | --- | --- |
| `src-tauri/` | 目录（≈31M） | Tauri 应用：`tauri.conf.json`、`capabilities/default.json`、`src/main.rs`、编译产物 `binaries/back-rust-*-apple-darwin`（Rust sidecar）、`icons/`、`gen/schemas/` |
| `back-rust/` | 目录（≈100K） | Rust 后端：`Cargo.toml`、`src/{main,ssh_service,types}.rs`、`Cargo.lock`、`target/`（构建缓存） |
| `scripts/prepare-tauri-sidecar.mjs` | 文件 | Tauri sidecar 打包脚本（含 `cargo`/`rustup`/`lipo` 调用），删除后 `scripts/` 目录一并移除 |
| `metadata.json` | 文件（4K） | 根目录孤儿清单，声明 `camera`/`microphone` 权限，不被任何构建/运行时代码引用 |

### 2. 修改的配置文件

- **`package.json`**：移除 `tauri:dev`、`tauri:build` 两条脚本；移除 `devDependencies` 中的 `@tauri-apps/api`、`@tauri-apps/cli`。保留全部 Electron 脚本与 `electron-builder` 配置不变。
- **`tsconfig.json`**：`exclude` 数组移除 `"back-rust"`、`"src-tauri"`。
- **`.gitignore`**：移除 `src-tauri/target`、`src-tauri/binaries/*`、`back-rust/target/*` 三条已失效忽略规则。

### 3. 修改的源码（消除 Tauri 死代码）

- **`components/AISSH/services/sshService.ts`**：删除 `isTauri` 检测分支（`window.location.protocol==="tauri:"`、`__TAURI__`、`__TAURI_INTERNALS__`），`socketUrl` 改为仅依据 `isElectron` 判断。
- **`index.css`**：滚动条注释 `（Tauri/WebKit）` → `（WebKit）`。

### 4. 依赖树同步

- 执行 `pnpm install --prefer-offline`，`node_modules/@tauri-apps/*` 已移除，`pnpm-lock.yaml` 中 `@tauri-apps` 条目归零。

### 5. 验证结果

- 全仓库（排除 `node_modules`/`dist`/`.pnpm-store`/lockfile/本报告/`.git`）grep `tauri|back-rust` 已无真实引用（命中仅 `dataURI` 误匹配与 `.git` 历史提交记录）。
- Electron 构建链路不受影响：其后端走 `back/dist/bundle`（Node），不依赖 `back-rust`。

### 6. 残留 / 待办说明

- **`dist/` 编译产物**含旧 Tauri 字符串（如 `vendor-ai-*.js`），属过期构建输出，下次 `vite build` / `electron:build` 会重新生成，无需手工处理。
- **`.git` 历史**仍保留早期 "feat: 添加Tauri桌面端支持" 等提交——删除工作区文件不会抹除历史，属正常，不影响当前代码。
- **C4 品牌命名**未完全解决：`metadata.json` 已删（少了一个名字），但 `package.json` 内 `name=bintelai-ssh-assistant`、`productName=方块SSH助手`、`appId=com.gemini.ssh.assistant` 仍未统一，留待后续单独处理。
- **未涉及**：A1 密码明文存储、B1 风险拦截未落地、D1 设计系统绕开、E1 全量订阅、E2 Scroll 泄漏等仍在整改清单中待处理。

---

## 🛠 Phase 1 优化执行记录（2026-08-22）

> 因 SoftwareCompany 团队子代理调度触发速率限制（重置于当日 18:09），由主理人直接执行 Phase 1 边界清晰项并完成验证。

### 1. A1 密码 / AI Key 加密落盘（P0）

- 新增 `back/src/database/crypto.util.ts`：AES-256-GCM；密钥来自环境变量 `AISSH_DB_KEY`（hex/base64 32 字节）或 app data 目录 `.dbkey` 随机密钥文件（权限 0600）；密文带 `enc:v1:` 前缀。
- `DatabaseService`：构造期建密钥库并执行 `migrateEncryptPasswords()`（一次性加密历史明文，幂等）；`findServer`/`getServerPasswords` 解密；新增 `encryptPassword`/`decryptPassword`。
- `ConfigurationService`：`replace()` 写入点对**新密码**加密、**存量密文**原样保留；`saveServerCredential` 加密后入库。
- `CredentialService`：`setApiKey` 加密、`getApiKey` 解密（AI Key 同步加密）。
- 兼容遗留明文：解密遇无前缀值原样返回；解密失败返回 null（触发重新输入，不发送乱码到 SSH）。
- 测试：`configuration.service.spec.ts`、`credential.service.spec.ts` 断言改为"密文存储 + 解密往返原文"。

### 2. B1 命令风险拦截真正落地（P0）

- `geminiService.ts`：导出 `isRiskyCommand`（原模块私有）。
- `CommandInput.tsx`：拆出 `performExecute`；`handleExecuteCommand` 执行前用 `isRiskyCommand` 判定，命中高危即弹出 sci 风格二次确认框（展示命令 + 取消/确认执行），确认后才执行。
- 静态规则零网络往返，主通道强制确认；原"AI 风险评估"按钮保留为可选深度分析。

### 3. E2 Scroll 监听泄漏修复（P2）

- `SessionTabs.tsx`：提取稳定 `handleScroll = () => setMenu(null)` 引用，注册与清理用同一函数，彻底消除监听无法移除的泄漏。

### 4. E4 i18n 死重移除（P3）

- `package.json`：移除 `i18next`、`react-i18next`、`i18next-browser-languagedetector`；`vite.config.ts` `vendor-utils` 分包去掉 i18n 项。
- `pnpm install` 后 lockfile 与 `node_modules` 中 i18n 条目归零。

### 5. C4/C5 命名与文档（P2）

- `package.json`：`appId` 由 `com.gemini.ssh.assistant` 统一为 `com.bintelai.ssh.assistant`。
- `README.md`：清理品牌乱码（标题"方块 SSH 助手SSH 助手"→"方块 SSH 助手"；正文"Google 方块 SSH 助手AI 能力"→"AI 能力"；SDK 描述清理）。
- `TECHNICAL_DOC.md`：`loadURL('http://localhost:5173')` → `3000`。

### 6. 验证

- 后端：`npx tsc --noEmit` 通过；`pnpm test`（`NODE_OPTIONS=--experimental-sqlite`）**29/29 测试通过**。
- 前端：`npx tsc --noEmit` 通过。
- 依赖：`node_modules` 中 `i18next`/`react-i18next` 已移除。

### 7. 推迟项（Phase 2 / 专项）

- ~~**E1 Zustand 全量订阅**：需引入 `useShallow` 逐文件改造~~ **✅ 已于本轮处理（见下方 Phase 1.5）**
- ~~**D1 设计系统配色扫荡**：纯 cosmetic、多文件~~ **✅ 已于本轮处理（见下方 Phase 1.5）**
- **D2 拆分 1500+ 行大文件**：高风险大型重构，留作专项。

---

## 🎨 Phase 1.5：E1 选择器 + D1 配色扫荡（2026-08-22）

> 续 Phase 1 后继续优化，子代理速率限制未解除，仍由主理人直接执行+验证。

### 1. E1 Zustand 全量订阅 → 选择器 + useShallow（P2）

- `AISSH.tsx`（根组件，原订阅 3 store）：`useSSHStore`/`useFileStore`/`useMultiIPStore` 全部改为 `useShallow` 拆分「状态/动作」两组选择器。根组件此前因整库订阅，**每条终端日志（addLog）都触发整树重渲染**，改造后仅在所选状态变化时重渲染。
- `AIChatPanel.tsx`（原订阅 4 store，1569 行）：`useAIStore`/`useSSHStore`/`usePromptStore`/`useMultiIPStore` 同样改 `useShallow` 拆分。
- 动作（actions）经 `useShallow` 选择后因稳定引用浅比较相等，不再触发重渲染。

### 2. D1 设计系统配色全量扫荡（P1）

- **Tailwind 违规色**：`slate-*`→`white/10` 等语义边框；`sky-400`→`sci-cyan`；`red-500/400`→`sci-red`；`green-500/400`→`sci-green`；`yellow-*`/`orange-*`→`sci-violet`（sci 无黄/橙，以 violet 作告警色）。
- **硬编码 GitHub-dark 色**：`#0d1117`→`sci-obsidian`/`#11161d`；`#161b22`→`#1a2129`；`#0a1622`→`sci-obsidian`；`#030507`→`sci-base`；`#1a1f2e`→`#11161d`；`#22c55e`→`#0aff00`（sci-green）。
- **覆盖文件**：`AISSH`、`Button`、`CyberPanel`、`CyberSelect`、`Terminal`、`MonacoEditor`、`MultiIPOperationCenter`（重灾区，十余处状态色）、`AIEmptyState`、`ServerTree`、`HackerStandby`、`FileTabs`、`FileBrowser`、`AIChatPanel`、`CyberBackground`。
- **保留**：终端 xterm ANSI 调色板（red/green/yellow/cyan 与 sci 一致，按报告可保留）；按钮 hover 浅色 hex（`#66f6ff` 等，属意图性提亮）。
- 验证：`components/` 下 `slate/sky/red-5xx/green-5xx/yellow/orange` 与上述硬编码色 grep 已无残留。

### 3. 验证

- 前端 `npx tsc --noEmit` 通过（E1 选择器重构与 D1 配色替换均无类型错误）。

### 4. 仍推迟

- **D2 拆分 `AIChatPanel.tsx`(1569 行)/`MultiIPOperationCenter.tsx`(742 行) 等**：高风险大型重构，留作专项，不勉强一次性做。
